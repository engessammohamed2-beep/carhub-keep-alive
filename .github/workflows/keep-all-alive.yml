name: Keep All 3 Projects Alive
on:
  schedule:
    - cron: '0 9 */2 * *'
  workflow_dispatch:
jobs:
  keep-alive:
    runs-on: ubuntu-latest
    steps:
      - name: Ping All Projects
        run: |
          echo "Pinging CarHub..."
          curl -s https://bnmaqdrwnnbqiivhcdns.supabase.co/ > /dev/null && echo "CarHub OK" || echo "CarHub fail but continue"
          echo "Pinging idea-360..."
          curl -s https://agxoxchosqctlcojwehd.supabase.co/ > /dev/null && echo "idea-360 OK" || echo "idea-360 fail but continue"
          echo "Pinging MOHTSBE..."
          curl -s https://avfooxzwzlvmxockdngv.supabase.co/ > /dev/null && echo "MOHTSBE OK" || echo "MOHTSBE fail but continue"
          echo "All done - 3 projects pinged!"
